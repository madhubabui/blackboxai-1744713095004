<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme -->
    <style name="Theme.Saver.Splash" parent="Theme.Saver.NoActionBar">
        <item name="android:windowBackground">@color/primary</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/primary</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Base NoActionBar Theme -->
    <style name="Theme.Saver.NoActionBar" parent="Theme.Saver">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <!-- Full Screen Theme -->
    <style name="Theme.Saver.FullScreen" parent="Theme.Saver.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- Dialog Theme -->
    <style name="Theme.Saver.Dialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:windowBackground">@color/surface</item>
        <item name="buttonBarPositiveButtonStyle">@style/Widget.Saver.Button.TextButton.Dialog</item>
        <item name="buttonBarNegativeButtonStyle">@style/Widget.Saver.Button.TextButton.Dialog</item>
    </style>

    <!-- Button Styles -->
    <style name="Widget.Saver.Button.TextButton.Dialog" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">?attr/colorPrimary</item>
        <item name="rippleColor">?attr/colorPrimary</item>
    </style>

    <!-- Bottom Sheet Dialog Theme -->
    <style name="Theme.Saver.BottomSheetDialog" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.Saver.BottomSheet</item>
    </style>

    <style name="Widget.Saver.BottomSheet" parent="Widget.MaterialComponents.BottomSheet">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.Saver.LargeComponent</item>
        <item name="behavior_peekHeight">512dp</item>
    </style>

    <!-- Shape Appearances -->
    <style name="ShapeAppearance.Saver.LargeComponent" parent="ShapeAppearance.MaterialComponents.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">16dp</item>
    </style>
</resources>
